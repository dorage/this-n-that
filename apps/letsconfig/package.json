{
  "name": "letsconfig",
  "type": "module",
  "version": "0.0.1",
  "description": "load config file and return type-safe object",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && npx tsc --project tsconfig.build.json && esbuild dist/tsc/index.js --minify --outfile=dist/index.js --bundle --platform=node",
    "test": "pnpm jest"
  },
  "keywords": [
    "cli",
    "configuration",
    "config",
    "utility"
  ],
  "author": "https://github.com/dorage",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/lodash": "^4.17.4",
    "@types/node": "^20.12.12",
    "esbuild": "^0.21.4",
    "jest": "^29.7.0",
    "lodash": "^4.17.21",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "tsx": "^4.11.0",
    "typescript": "^5.4.5",
    "zod": "^3.23.8"
  }
}
